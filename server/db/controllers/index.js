import courses from './courses';
import auth from './authentification';
import users from './users';
import messages from './messages';

export { courses, auth, users, messages };
export default { courses, auth, users, messages };
